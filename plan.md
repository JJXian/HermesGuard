# HermesGuard 实现计划

> 项目定位：基于 Hermes Agent 的电商智能巡检平台，优先交付可演示、可测试、可解释的 MVP。
>
> 推进原则：先跑通最小技术链路，再完成确定性业务闭环，最后叠加 Agent、通知、可视化与工程化能力。

## 1. 实施原则

- MVP 只实现 `R001 付款超过 24 小时未发货`，其他规则放入增强阶段。
- 规则引擎负责事实判断，Hermes Agent 负责上下文查询、解释和报告生成。
- 每个阶段都必须有可运行产物、自动化测试和明确验收条件。
- 外部模型、Hermes 或通知渠道失败时，确定性巡检结果仍必须保存。
- 先采用模块化单体代码库，通过 API、Worker、Beat、MCP 独立进程隔离运行职责。
- 性能和质量数据必须由测试报告产生，不提前写成已实现结果。

## 2. 目标仓库结构

```text
HermesGuard/
├── apps/
│   ├── server/                  # FastAPI、领域模块和 Celery Worker
│   ├── web/                     # Vue 3 管理后台
│   └── mcp-server/              # HermesGuard MCP Server
├── hermes/
│   ├── skills/ecommerce-inspection/
│   ├── prompts/
│   └── examples/
├── deploy/
│   ├── docker-compose.yml
│   ├── prometheus/
│   └── grafana/
├── docs/
│   ├── adr/
│   ├── benchmark/
│   └── images/
├── mock-data/
├── scripts/
├── tests/
├── .env.example
├── pyproject.toml
└── README.md
```

## 3. 分阶段路线图

### 阶段 0：Hermes 与 MCP 最小技术验证

**目标**

在正式搭建业务系统前，验证 Hermes、Skill、MCP 和结构化报告链路可行。

**任务**

- [x] 检查本机 Python、uv、Node、Docker 和 Hermes 环境。
- [x] 创建最小 `ecommerce-inspection` Skill。
- [x] 定义第一版巡检报告 JSON Schema。
- [x] 编写一个返回固定模拟执行结果的 MCP 工具。
- [x] 编写报告 Schema 校验脚本和测试样例。
- [x] 让 Hermes 调用 MCP 工具并输出结构化报告。
- [x] 记录真实运行方式、限制和失败结果。

**验收标准**

```text
Hermes → 调用 MCP → 获取模拟异常 → 生成报告 → Schema 校验通过
```

模型或 Hermes 不可用时，应保留可重复执行的本地 MCP 与 Schema 校验测试。

### 阶段 1：项目框架与基础设施

**目标**

建立可持续开发的 Monorepo 和本地运行环境。

**任务**

- [ ] 初始化 Python 3.12、uv workspace 和基础依赖。
- [ ] 初始化 Vue 3、TypeScript、Vite 和 Element Plus。
- [ ] 创建 FastAPI、Celery 和 MCP Server 进程入口。
- [ ] 创建 MySQL、Redis 的 Docker Compose 配置。
- [ ] 配置 Ruff、mypy、pytest、ESLint 和前端构建检查。
- [ ] 添加 `.gitignore`、`.env.example` 和统一开发命令。
- [ ] 添加最小 CI：静态检查、单元测试、前端构建。

**验收标准**

- API、Web、MySQL、Redis 可启动。
- `/health/live` 与 `/health/ready` 正常。
- 前端能够访问后端健康接口。
- 空框架 CI 全部通过。

### 阶段 2：后端公共能力

**目标**

完成业务模块共用的配置、数据访问和诊断能力。

**任务**

- [ ] FastAPI 应用工厂与模块路由注册。
- [ ] Pydantic Settings 环境配置。
- [ ] SQLAlchemy 2.0 Session 生命周期。
- [ ] Alembic 数据库迁移。
- [ ] 统一响应、错误码、异常处理和分页结构。
- [ ] `traceId`、JSON 结构化日志和敏感字段过滤。
- [ ] 单元测试与集成测试基础设施。

**验收标准**

- 可以执行首个数据库迁移。
- 成功和错误响应结构统一。
- 请求日志包含 `traceId` 且不泄露配置密钥。

### 阶段 3：登录、店铺、模拟订单与 R001 规则

**目标**

不依赖 Agent，先证明巡检事实计算准确。

**任务**

- [ ] 实现 `ADMIN`、`OPERATOR`、登录和 JWT 鉴权。
- [ ] 实现店铺 CRUD、启停与连接测试。
- [ ] 定义 `OrderProvider` 接口和 Mock 实现。
- [ ] 生成正常、超时、边界和脏数据订单。
- [ ] 实现 R001 超时未发货规则、参数和版本快照。
- [ ] 实现规则测试接口，不产生正式异常。
- [ ] 覆盖边界、时区、脏数据和状态组合测试。

**验收标准**

```text
创建模拟店铺 → 生成订单 → 规则测试 → 返回准确匹配结果
```

其中 `23:59:59` 不命中，`24:00:00` 命中，已发货订单不命中。

### 阶段 4：巡检任务和执行流水线

**目标**

将单次规则测试升级为可管理、可调度、可追踪的巡检任务。

**任务**

- [ ] 巡检任务 CRUD、启停与逻辑删除。
- [ ] 手动触发并返回 `202 + executionId`。
- [ ] Celery Worker、Beat 和动态到期任务分发。
- [ ] 阶段化执行状态及阶段日志。
- [ ] 调度、手动触发幂等键与数据库唯一约束。
- [ ] 同任务运行互斥和 `SKIP/QUEUE` 策略。
- [ ] 阶段超时、错误码和卡死任务收敛。

**验收标准**

- 手动触发和周期触发均可运行。
- 相同幂等键并发触发只产生一个 execution。
- 执行详情能够定位失败阶段、耗时和错误原因。

### 阶段 5：异常去重、状态与恢复闭环

**目标**

完成不依赖 AI 的首个完整业务闭环。

**任务**

- [ ] 异常指纹、唯一索引和并发 Upsert。
- [ ] `anomaly_occurrence` 与执行关联。
- [ ] OPEN、ACKNOWLEDGED、RESOLVED、IGNORED 状态机。
- [ ] 首次发现、最近发现、累计命中和重新打开。
- [ ] 自动恢复策略。
- [ ] 状态变更审计。
- [ ] 异常列表、详情、筛选和状态操作 API。

**验收标准**

```text
首次巡检创建异常
→ 重复巡检更新同一异常
→ 订单发货后自动恢复
→ 再次命中后重新打开
```

### 阶段 6：Hermes、MCP 与 AI 报告

**目标**

在确定性闭环之上加入受控、可审计、可降级的智能分析。

**任务**

- [ ] MCP 服务身份认证、参数校验、权限与店铺范围校验。
- [ ] 实现执行结果、异常、订单快照等只读工具。
- [ ] 实现受限的 `save_agent_report` 工具。
- [ ] 完成 `ecommerce-inspection` Skill 和提示词版本管理。
- [ ] 实现报告 JSON Schema 与事实一致性校验。
- [ ] 非法输出修复一次，仍失败则生成模板报告。
- [ ] 保存模型、Token、耗时和脱敏工具轨迹。
- [ ] 加入 Prompt 注入和越权工具调用测试。

**验收标准**

- Hermes 只能调用白名单工具。
- 报告中的数量、订单 ID 和时间与规则结果一致。
- Hermes 或模型不可用时生成模板报告，执行状态为 `PARTIAL_SUCCESS`。

### 阶段 7：通知与 Outbox

**目标**

让异常、降级结果和恢复事件可靠触达运营人员。

**任务**

- [ ] 优先实现飞书机器人，保留 SMTP 扩展接口。
- [ ] 通知渠道配置、加密和连接测试。
- [ ] 通知模板和详情链接。
- [ ] Outbox 事件写入与消费。
- [ ] 通知记录唯一键和发送幂等。
- [ ] 指数退避、死信和人工重发。
- [ ] 异常恢复通知。

**验收标准**

- 异常报告、降级报告和恢复事件均可发送。
- 重复消费不创建重复通知记录。
- 模拟限流后能够重试并保留诊断记录。

### 阶段 8：Vue 管理后台

**目标**

贯通五分钟现场演示的全部操作路径。

**页面顺序**

1. 登录。
2. 店铺管理与模拟数据生成。
3. 巡检任务列表与创建。
4. 执行历史与阶段详情。
5. 异常列表、详情与状态操作。
6. AI 报告与工具轨迹。
7. 工作台概览与趋势。
8. 通知渠道与审计日志。

**验收标准**

- 可以从页面完成创建店铺、注入异常、创建任务、执行、查看报告和恢复异常。
- 页面明确区分规则事实、AI 推断、不确定性和降级结果。

### 阶段 9：安全、可观测性与质量

**目标**

把“可以运行”提升为“可以证明其正确性和工程质量”。

**任务**

- [ ] RBAC 越权测试和服务身份隔离。
- [ ] 凭证 AES-GCM 加密和日志脱敏。
- [ ] Prometheus 指标、OpenTelemetry 链路和 Grafana 面板。
- [ ] Testcontainers 集成测试和 Playwright 核心 E2E。
- [ ] 建立不少于 50 条版本化 AI 评测集。
- [ ] 完成至少五种故障注入。
- [ ] Secret 和依赖安全扫描。
- [ ] 保存可复现的质量与故障测试报告。

**验收标准**

- 幂等、降级、权限和事实一致性均有自动化测试证明。
- 所有质量和性能结论都带环境、样本量、版本和运行时间。

### 阶段 10：部署与作品集包装

**目标**

形成可交付、可演示、可写入简历的完整作品。

**任务**

- [ ] 完整 Docker Compose 与 Nginx 配置。
- [ ] 初始化数据和一键演示脚本。
- [ ] README、架构图、ER 图、API 文档和 ADR。
- [ ] 压测报告和 AI 评测报告。
- [ ] Grafana、故障注入和核心链路截图。
- [ ] 录制 3～5 分钟演示视频。
- [ ] 根据真实实现与测试结果整理简历和面试讲解稿。

**验收标准**

- 新环境按 README 可在 15 分钟内启动演示。
- 演示无需临时修改数据库。
- 简历中的每项成果都有代码、测试或报告作为依据。

## 4. 里程碑

| 里程碑 | 交付结果 |
|---|---|
| M1 | Hermes、MCP、Skill 与 Schema 最小链路跑通 |
| M2 | 项目框架、数据库、前后端和基础设施可启动 |
| M3 | R001 能准确识别超时订单 |
| M4 | 任务、执行、异常和恢复形成确定性闭环 |
| M5 | Hermes 经 MCP 生成经过校验的结构化报告 |
| M6 | 通知和前端形成完整演示闭环 |
| M7 | 测试、监控、安全、部署和作品集完成 |

## 5. 推荐节奏

| 周期 | 重点 |
|---|---|
| 第 0 周（2～3 天） | Hermes、Skill、MCP 技术验证 |
| 第 1 周 | 框架、公共能力、模拟数据与 R001 |
| 第 2 周 | 任务、调度、执行流水线和异常闭环 |
| 第 3 周 | MCP、Hermes 深度集成、报告校验和降级 |
| 第 4 周 | 通知、管理后台和核心演示流程 |
| 第 5 周 | 安全、测试、AI 评测、故障注入和可观测性 |
| 第 6 周 | 一键部署、压测、文档、视频和作品集包装 |

## 6. 当前进度

- [x] 完成需求规格说明书梳理。
- [x] 确认仓库为全新空目录。
- [x] 制定分阶段实现计划。
- [ ] 正在执行阶段 0：Hermes 与 MCP 最小技术验证。
- [x] 阶段 0 本地 Skill、MCP、Schema 和自动化测试已完成。
- [ ] 阶段 0 真实模型调用等待用户明确授权发送模拟数据并承担少量 API 费用。
