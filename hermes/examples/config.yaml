# 安装 Hermes 后，将此配置合并到 ~/.hermes/config.yaml。
mcp_servers:
  hermesguard:
    command: "uv"
    args:
      - "run"
      - "--project"
      - "/absolute/path/to/HermesGuard/apps/mcp-server"
      - "mcp"
      - "run"
      - "/absolute/path/to/HermesGuard/apps/mcp-server/src/hermesguard_mcp/server.py"
    timeout: 10
    tools:
      include: ["get_execution_result"]
      prompts: false
      resources: false
